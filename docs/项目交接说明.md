# 项目交接说明

本文档用于 AI 模型或开发者快速了解项目状态、开发流程和注意事项。

## 1. 项目概况

```text
项目目录：/Users/l/Documents/Codex/wms-print-template-center
GitHub 仓库：git@github.com:794280883-ops/Print_Template.git
当前分支：main
```

技术栈：

```text
前端：Vite + Vanilla JavaScript
后端：Node.js + Express
数据库：MySQL
部署：Docker Compose + Nginx
```

## 2. 本地开发流程

```text
1. 启动 Docker Desktop 中的 MySQL 容器
2. 执行 ./scripts/dev-local.sh
3. 访问 http://127.0.0.1:5173
```

详细说明见 [本地开发指南](本地开发指南.md)。

## 3. 业务数据模块

业务数据支持：

```text
查询、新增、编辑、删除
业务数据打印选择、预览
Excel 导入
```

业务类型：

```text
LOCATION  库位管理
CONTAINER 容器管理
PRODUCT   商品管理
```

## 4. 字段映射

后端映射配置：

```text
backend/src/config/businessDataMapping.js
```

当前映射：

```text
LOCATION -> location
  locationCode   -> location_code       必填
  locationPrefix -> location_prefix
  row            -> location_row
  column         -> location_column
  level          -> location_floor
  directionMark  -> direction_flag
  warehouseCode  -> region_warehouse_code  
  areaCode       -> physics_warehouse_code

CONTAINER -> container
  containerCode  -> container_code      必填
  warehouseCode  -> region_warehouse_code  
  areaCode       -> physics_warehouse_code

PRODUCT -> product
  productCode         -> sku            必填
  customerProductCode -> customer_code
```

方向标规则：

```text
数据库 direction_flag = 1 -> 显示：向上 -> 打印：↑
数据库 direction_flag = 2 -> 显示：向下 -> 打印：↓
数据库 direction_flag = 空 -> 显示空 -> 打印空
```

## 5. 服务器部署

服务器：

```text
公网 IP：47.113.118.74
部署目录：/opt/wms-print-template-center
```

一键部署：

```bash
./scripts/deploy-server.sh
```

详细说明见 [服务器部署指南](服务器部署指南.md)。

## 6. 安全规范

不要提交到 Git：

```text
.env
.env.test
.env.production
真实数据库密码
服务器 root 密码
SSH 私钥
公司内网数据库连接信息
```

提交前检查：

```bash
./scripts/check-before-push.sh
```

## 7. 给后续 AI 模型的建议

继续开发前先执行：

```bash
cd /Users/l/Documents/Codex/wms-print-template-center
git status --short --branch
```

常用操作：

```text
本地调试：./scripts/dev-local.sh
提交前检查：./scripts/check-before-push.sh
部署服务器：./scripts/deploy-server.sh
```

## 8. 后续建议

```text
1. 完成 customprint.icu ICP 备案
2. 备案完成后配置 HTTPS
3. 业务数据新增/编辑/删除加鉴权
4. 业务数据删除改为软删除
5. 迁移脚本引入 migration history 表
6. 准备固定 SSH key，不依赖 /tmp/wms_deploy_key
```
