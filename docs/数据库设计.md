---
title: 数据库设计
category: 技术文档
audience: developer
last_updated: 2026-07-03
tags: [database, schema, migration, RBAC, field-dict]
---

# 数据库设计

> **何时查看**：新增表或字段时 / 新增 migration 时 / 排查数据查询问题时 / 理解业务数据 JSON 存储模型时

本文档说明 打印模板中心的数据库结构。

## 数据库信息

```text
Database: wms_print_template
```

迁移脚本目录：

```text
backend/migrations/
```

迁移文件（按编号顺序执行）：

| 文件 | 说明 |
|------|------|
| `001_init.sql` | 核心 schema + 业务模块/字段/模板种子数据 |
| `002_rbac_tables.sql` | RBAC 五表权限系统 + 菜单/角色/用户种子数据 |
| `003_drop_business_record_unique.sql` | 移除 business_record 唯一约束（允许重复编码导入） |
| `004_add_field_unique.sql` | print_field_dict 增加 is_unique 列（字段级唯一性校验） |
| `005_expand_print_log_business_no.sql` | 扩大 print_log.business_no 列为 TEXT，支持大批量打印（原 VARCHAR(128) 仅能存约 5 条编码） |
| `006_add_business_record_print_count.sql` | business_record 增加 print_count 列，并为各业务模块增加系统字段「打印次数」 |

## 数据表

| 表名 | 说明 |
|------|------|
| print_template | 打印模板主表 |
| print_template_element | 模板元素（组件） |
| print_field_dict | 字段字典（按 module_code 分组） |
| print_business_module | 业务模块元数据 |
| business_record | 业务记录统一表（JSON 存储） |
| print_log | 打印日志 |
| operation_log | 操作日志 |
| sys_user | 用户 |
| sys_role | 角色 |
| sys_menu | 菜单与按钮权限 |
| sys_user_role | 用户-角色关联 |
| sys_role_menu | 角色-菜单关联 |

## business_record 统一表

所有业务数据（库位/容器/商品/自定义模块）统一存储于此表：

```sql
CREATE TABLE business_record (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  module_code VARCHAR(32) NOT NULL,        -- LOCATION/CONTAINER/PRODUCT/自定义
  record_code VARCHAR(128) NOT NULL,        -- 业务主键编码值
  record_data JSON NOT NULL,               -- 全部字段键值对
  search_text TEXT NULL,                   -- 可搜索字段值拼接，keyword 模糊搜索会同时匹配此列
  status TINYINT NOT NULL DEFAULT 1,
  print_count INT NOT NULL DEFAULT 0,       -- 成功打印次数，每条记录每次成功打印加 1
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_module_code (module_code),
  INDEX idx_module_print_count (module_code, print_count)
);
-- 注意：003 迁移已移除 uk_module_record 唯一约束，允许同一模块内编码重复
-- 编辑/删除通过 DB 主键 id 精确定位行，不受编码重复影响
-- 导入时逐行创建，编码字段默认不参与唯一性校验
```

## print_template 模板主表

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| template_code | VARCHAR(64) UNIQUE | 模板编码 |
| template_name | VARCHAR(128) | 模板名称 |
| template_type | VARCHAR(32) | 模板类型（LOCATION/CONTAINER/PRODUCT/自定义） |
| width_mm | DECIMAL(10,2) | 宽度(mm) |
| height_mm | DECIMAL(10,2) | 高度(mm) |
| unit | VARCHAR(16) | 单位（默认mm） |
| dpi | INT | 打印DPI（默认203） |
| print_rotation | INT | 打印旋转角度（0/90/180/270） |
| status | VARCHAR(32) | 状态（enabled/disabled） |
| remark | VARCHAR(512) | 备注 |
| field_preview_values | JSON | 预览时的模拟字段值 |
| updated_at | DATETIME | 更新时间（自动更新） |

## print_log 打印日志

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| template_id | BIGINT | 关联模板ID |
| template_code | VARCHAR(64) | 模板编码 |
| business_type | VARCHAR(32) | 业务类型 |
| business_no | TEXT | 业务编码拼接值（005 迁移后扩大，后端最多保留 2048 字符） |
| warehouse_code | VARCHAR(64) | 区域仓编码 |
| print_payload | JSON | 打印请求载荷 |
| print_status | VARCHAR(32) | 打印状态 |
| error_message | VARCHAR(1024) | 错误信息 |
| operator | VARCHAR(64) | 操作人 |
| printed_at | DATETIME | 打印时间 |

## print_business_module

```sql
CREATE TABLE print_business_module (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  module_code VARCHAR(32) NOT NULL,
  module_name VARCHAR(128) NOT NULL,
  template_label VARCHAR(128) NOT NULL,
  data_label VARCHAR(128) NOT NULL,
  record_code_field VARCHAR(64) NOT NULL,  -- 主键字段编码
  storage_mode VARCHAR(32) NOT NULL DEFAULT 'json_table',
  enabled TINYINT(1) NOT NULL DEFAULT 1,
  sort_no INT NOT NULL DEFAULT 0,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_module_code (module_code)
);
```

内置模块：LOCATION（库位）、CONTAINER（容器）、PRODUCT（商品），storage_mode 统一为 `json_table`。

## RBAC 权限系统

五表标准 RBAC 模型，用户 → 角色 → 菜单权限。

```
sys_user ──多对多── sys_user_role ── sys_role
                                        │
                                   sys_role_menu
                                        │
                                     sys_menu (directory/page/button)
```

- 登录 `/auth/login` 返回 token + 权限码列表 + 菜单树
- 前端路由守卫 + v-permission 指令控制页面和按钮
- roles/menus CRUD 完整接口

## 种子数据

迁移后自动写入：

```text
业务模块：
- LOCATION   库位
- CONTAINER  容器
- PRODUCT    商品

预置模板：
- TPL_LOCATION_10050      库位标签-100x50
- TPL_CONTAINER_IN_10050  容器入库标签-100x50
- TPL_PICKING_100150      拣货容器标签-100x150
```

## 字段字典定义

字段存储在 `print_field_dict`，按 `module_code` 分组。运行时由 `schemaService.compileSchema(moduleCode)` 动态读取，无需硬编码映射。

完整字段结构：

| 列名 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| module_code | VARCHAR(32) | 所属模块编码 |
| field_code | VARCHAR(128) | 字段编码 |
| field_name | VARCHAR(128) | 字段中文名称 |
| field_type | VARCHAR(32) | 字段类型（string/number/integer/date/select） |
| example_value | VARCHAR(512) | 示例值 |
| is_required | TINYINT(1) | 是否必填 |
| description | VARCHAR(512) | 字段说明 |
| sort_no | INT | 排序号 |
| enabled | TINYINT(1) | 是否启用 |
| searchable | TINYINT | 是否启用查询 |
| sortable | TINYINT | 是否支持排序 |
| bindable_in_template | TINYINT | 是否可在模板中绑定（默认1） |
| is_unique | TINYINT(1) | 是否要求值唯一（004迁移新增，默认0） |

`searchable` 列控制该字段是否在业务数据中支持精确查询（1=启用，0=不启用）。业务编码字段（主键）默认 `searchable=1` 且不可取消。

系统字段 `printCount / 打印次数` 由迁移自动为每个业务模块创建，`bindable_in_template=0`，不进入新增、编辑、导入和模板绑定流程；字段页只用于配置该列是否显示、排序号和是否支持排序。实际计数存储在 `business_record.print_count`。

### 库位（LOCATION）

| 字段编码 | 名称 | 必填 | 可搜索 | 可排序 |
|---------|------|------|--------|--------|
| locationCode | 库位编码 | 是 | 是 | 是 |
| locationPrefix | 库位前缀 | — | 是 | 是 |
| row | 排 | — | — | — |
| column | 列 | — | — | — |
| level | 层 | — | — | — |
| directionMark | 方向标 | — | — | — |
| warehouseCode | 区域仓编码 | 是 | 是 | 是 |
| areaCode | 物理仓编码 | — | 是 | 是 |

### 容器（CONTAINER）

| 字段编码 | 名称 | 必填 | 可搜索 | 可排序 |
|---------|------|------|--------|--------|
| containerCode | 容器编码 | 是 | 是 | 是 |
| warehouseCode | 区域仓编码 | — | 是 | 是 |
| areaCode | 物理仓编码 | — | 是 | 是 |

### 商品（PRODUCT）

| 字段编码 | 名称 | 必填 | 可搜索 | 可排序 |
|---------|------|------|--------|--------|
| productCode | 商品编码 | 是 | 是 | 是 |
| ProductBarcode | 商品条码 | — | 是 | 是 |
| customerProductCode | 客户商品编码 | — | 是 | 是 |

### 方向标规则

```
direction_flag = 1  → 显示"向上" → 打印 ↑
direction_flag = 2  → 显示"向下" → 打印 ↓
direction_flag = 空 → 显示空 → 打印空
```

## 存储说明

所有业务数据字段以 JSON 存储在 `business_record.record_data` 中。搜索时：
- **关键字搜索**：按 `record_code` 和 `search_text` 模糊匹配，支持逗号/空格分隔批量查询
- **字段精确查询**：按 `JSON_EXTRACT(record_data, '$."字段编码"')` 精确匹配，仅对启用了查询（`searchable=1`）的字段生效

`search_text` 列为可搜索字段值的拼接，用于配合 `record_code` 做 keyword 模糊搜索。后端由 `schemaService` 运行时编译模块和字段 schema，`recordRepository` 统一执行 CRUD。

后续增量迁移从 006 开始编号，迁移文件列表及说明见上方「迁移文件」表格。
